"""PDF -> page image rendering.

TODO(migration): port `render_pdf_pages_to_base64` from
mafio/data/utils/visual_chunking.py, generalized to drop mafio-specific
naming (e.g. `safe_stem`/`to_chunk_document_id` stay chunking-side or move
to a shared `ids.py`).
"""
